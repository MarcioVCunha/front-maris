{
  "tasks": {
    "test": "deno test --no-check --allow-read \"**/*.test.ts\"",
    "test:cov": "deno test --no-check --allow-read --coverage=cov \"**/*.test.ts\"",
    "cov": "deno coverage cov"
  }
}
